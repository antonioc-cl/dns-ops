# Railway deployment configuration for DNS Ops monorepo
# This is for the web service

[build]
# Build using Bun workspaces - install all deps then build web
buildCommand = "bun install && bun run build --filter=@dns-ops/web"

[deploy]
startCommand = "/app/scripts/migrate.sh"
healthcheckPath = "/api/health"
healthcheckTimeout = 10
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
