/**
 * Finding Repository
 *
 * Manages persisted DNS and mail findings linked to snapshots.
 * Findings are generated by the rules engine and stored for auditing
 * and historical comparison.
 */
import type { IDatabaseAdapter } from '../database/simple-adapter.js';
import { type Finding, type NewFinding } from '../schema/index.js';
export declare class FindingRepository {
    private db;
    constructor(db: IDatabaseAdapter);
    /**
     * Find a finding by ID
     */
    findById(id: string): Promise<Finding | null>;
    /**
     * Find all findings for a snapshot
     */
    findBySnapshotId(snapshotId: string): Promise<Finding[]>;
    /**
     * Find findings for a snapshot filtered by severity
     */
    findBySnapshotIdAndSeverity(snapshotId: string, severities: string[]): Promise<Finding[]>;
    /**
     * Find findings by rule ID
     */
    findByRuleId(ruleId: string, snapshotId?: string): Promise<Finding[]>;
    /**
     * Find findings by type (e.g., 'dns.authoritative-failure', 'mail.spf-missing')
     */
    findByType(type: string, snapshotId?: string): Promise<Finding[]>;
    /**
     * Create a new finding
     */
    create(data: NewFinding): Promise<Finding>;
    /**
     * Create multiple findings
     */
    createMany(data: NewFinding[]): Promise<Finding[]>;
    /**
     * Mark a finding as acknowledged
     */
    markAcknowledged(id: string, acknowledgedBy: string): Promise<Finding | null>;
    /**
     * Mark a finding as a false positive
     */
    markFalsePositive(id: string, acknowledgedBy: string): Promise<Finding | null>;
    /**
     * Delete all findings for a snapshot
     * Used when re-evaluating a snapshot with a new ruleset
     */
    deleteBySnapshotId(snapshotId: string): Promise<number>;
    /**
     * Count findings for a snapshot by severity
     */
    countBySeverity(snapshotId: string): Promise<Record<string, number>>;
    /**
     * Check if a snapshot has any findings
     */
    hasFindings(snapshotId: string): Promise<boolean>;
}
//# sourceMappingURL=finding.d.ts.map