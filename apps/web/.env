# Local dev environment — same PostgreSQL instance used by the collector.
# Adjust credentials to match your local setup.
DATABASE_URL=postgresql://antonio@localhost:5432/dns_ops
COLLECTOR_URL=http://localhost:3001
